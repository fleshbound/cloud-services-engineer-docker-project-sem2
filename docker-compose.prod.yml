version: '3.9'

services:
  backend:
    image: ${DOCKER_USER}/docker-project-backend:latest
    container_name: momo-store-backend
    read_only: true
    networks:
      - backend-network
    restart: always
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8081/health"]
      interval: 30s 
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
      mode: replicated
      replicas: 2
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    env_file:
      - ./backend/.env.prod

  frontend:
    image: ${DOCKER_USER}/docker-project-frontend:latest
    container_name: momo-store-frontend
    networks:
      - frontend_network
    volumes:
      - frontend_static:/app/static
    restart: no

  nginx:
    image: ${DOCKER_USER}/docker-project-gateway:latest
    container_name: momo-store-gateway
    read_only: true
    networks:
      - backend_network
      - frontend_network
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_completed_successfully
    volumes:
      - frontend_static:/app/static:ro
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.3'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 128M
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - CHOWN

volumes:
  frontend_static:
    
networks:
  backend-network:
    driver: bridge
    internal: true
  frontend-network:
    driver: bridge
